<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Registrar clientes</title>
    <link rel="stylesheet" href="../../../../src/output.css" />
    <link
      rel="shortcut icon"
      href="https://raw.githubusercontent.com/JeanCardozo/audios/main/z.png"
    />

    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
  </head>
  <body
    class="bg-gray-100 flex flex-col justify-center items-center min-h-screen p-4"
  >
    <div
      class="register-form bg-white p-8 rounded-xl shadow-md w-[396px] max-w-full"
    >
      <div class="text-center mb-6">
        <img
          src="https://raw.githubusercontent.com/JeanCardozo/audios/main/logo.png"
          alt="Logo de la empresa"
          class="mx-auto h-16 mb-4"
        />
        <h1 class="text-[#1c1e21] text-3xl font-bold">Crear cuenta</h1>
        <p class="text-[#606770] mt-2">Es rápido y fácil.</p>
      </div>
      <form
        id="registerForm"
        action="/register"
        method="post"
        class="space-y-3"
      >
        <div class="flex space-x-2">
          <input
            type="text"
            id="nombre"
            name="nombre"
            placeholder="Nombre"
            required
            class="w-1/2 px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
          />
          <input
            type="text"
            id="apellido"
            name="apellido"
            placeholder="Apellido"
            required
            class="w-1/2 px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
          />
        </div>
        <input
          type="text"
          id="id"
          name="id"
          placeholder="Identificación"
          required
          class="w-full px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
        />
        <input
          type="email"
          id="correo"
          name="correo"
          placeholder="Correo electrónico"
          required
          class="w-full px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
        />
        <input
          type="tel"
          id="telefono"
          name="telefono"
          placeholder="Número de teléfono"
          required
          class="w-full px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
        />
        <input
          type="number"
          id="edad"
          name="edad"
          placeholder="Edad"
          min="0"
          max="100"
          required
          class="w-full px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
        />
        <input
          type="text"
          id="sexo"
          name="sexo"
          placeholder="Sexo e.j. Hombre"
          required
          class="w-full px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
        />
        <input
          type="password"
          id="pass"
          name="pass"
          placeholder="Contraseña nueva"
          required
          class="w-full px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
        />
        <input
          type="password"
          id="confirmarpass"
          name="confirmarpass"
          placeholder="Confirmar contraseña"
          required
          class="w-full px-3 py-2 bg-[#f5f6f7] border border-[#ccd0d5] rounded-md text-[15px] focus:outline-none focus:border-[#1877f2]"
        />
        <div class="text-xs text-[#777] mt-2">
          Al hacer clic en "Registrarte", aceptas nuestras Condiciones, la
          Política de privacidad y la Política de cookies.
        </div>
        <div class="flex justify-center mt-4">
          <input
            type="submit"
            value="Registrarte"
            class="px-16 py-2 bg-blue-600 text-white rounded-md text-xl font-bold cursor-pointer hover:bg-[#36a420] focus:outline-none focus:ring-2 focus:ring-[#00a400] focus:ring-offset-2"
          />
        </div>
      </form>
      <div class="text-center mt-4">
        <a href="/login_index" class="text-[#1877f2] hover:underline"
          >¿Ya tienes una cuenta?</a
        >
      </div>
    </div>

    <script>
      document
        .getElementById("registerForm")
        .addEventListener("submit", function (e) {
          const pass = document.getElementById("pass").value;
          const confirmarPass = document.getElementById("confirmarpass").value;

          if (pass !== confirmarPass) {
            e.preventDefault();
            Swal.fire({
              title: "Error",
              text: "Las contraseñas no coinciden",
              icon: "error",
              confirmButtonText: "OK",
            });
          }
        });
    </script>
    <% if (typeof alert !== "undefined") { %>
    <script>
      Swal.fire({
        title: "<%= alertTitle %>",
        text: "<%= alertMessage %>",
        icon: "<%= alertIcon %>",
        showConfirmButton: "<%= showConfirmButton %>",
        timer: "<%= timer %>",
      }).then(() => {
        window.location = "<%= ruta %>";
      });
    </script>
    <% } %>
  </body>
</html>
